---
- hosts: all
  become: true
  gather_facts: false

  pre_tasks:
    - name: Wait for SSH connection
      wait_for_connection:
        timeout: 300
        sleep: 5
        delay: 10

    - name: Gather facts
      setup:

  roles:
    - vm_security
